# Copy to api/.env and fill in. The file is git-ignored and read on startup.
# HS256 needs at least 32 bytes; generate one with (any shell, any OS):
#   node -e "console.log(require('node:crypto').randomBytes(48).toString('base64'))"
# or, where it is installed, openssl rand -base64 48
JWT_SECRET=
